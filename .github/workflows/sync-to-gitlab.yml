name: Sync Repository to GitLab

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - name: Sync to GitLab
      uses: keninkujovic/gitlab-sync@2.1.0
      with:
        gitlab_url: ${{ https://gitlab.com/santhosh1701/devops_assignment.git }}
        username: ${{ santhosh1701 }}
        gitlab_pat: ${{ glpat-odVwymRqxJPxyp_nhxhM }}
